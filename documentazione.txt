this.history = []; // questo segna tutto come events (punti,card,sub,timeout)

this.history.push({
      typeEvent: typeEvent,
      squadToSet: this.servingSquad,
      playerToSet: this.servingSquad.servingPlayer,
      touchOfPlayers: this.currentSelectedPlayer, 
      playerId: player.id,
      squadWhoWinPoint: player.team,
      pointMode: value,
      isAce: isAce,
      timestamp: Date.now(),
});

this.pointHistory = []; //per ora SOLO con i punti

dashboard --> per segnare i punti
dashboard_sets --> per vedere le statistiche dei singoli player nel match o singolo set
dashboard_squad --> per vedere le statistiche delle singole squad nel match o singolo set



<div id="lineup-toast" class="toast hidden">
    <div class="toast-content">

        <!-- SINISTRA: LISTA GIOCATORI -->
        <div class="players-list" id="available-players"></div>

        <!-- DESTRA: META CAMPO -->
        <div class="mini-court">

            <div class="court-grid">
                <div class="slot" data-pos="4"></div>
                <div class="slot" data-pos="3"></div>
                <div class="slot" data-pos="2"></div>

                <div class="slot" data-pos="5"></div>
                <div class="slot" data-pos="6"></div>
                <div class="slot" data-pos="1"></div>
            </div>

            <div class="toast-actions">
                <button id="cancel-lineup">Annulla</button>
                <button id="confirm-lineup">Conferma</button>
            </div>

        </div>

    </div>
</div>

<style>
    .toast {
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.6);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 9999;
    }

    .toast.hidden {
        display: none;
    }

    .toast-content {
        width: 900px;
        height: 500px;
        background: #111;
        display: flex;
        padding: 20px;
        border-radius: 12px;
        gap: 20px;
    }

    /* SINISTRA */
    .players-list {
        width: 40%;
        background: #1c1c1c;
        overflow-y: auto;
        padding: 10px;
        border-radius: 10px;
    }

    .player-item {
        background: #333;
        padding: 10px;
        margin-bottom: 8px;
        border-radius: 8px;
        cursor: grab;
    }

    /* DESTRA */
    .mini-court {
        width: 60%;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

    .court-grid {
        flex: 1;
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        grid-template-rows: repeat(2, 1fr);
        gap: 10px;
    }

    .slot {
        background: #222;
        border: 2px dashed #555;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .slot.filled {
        border-style: solid;
    }

    .toast-actions {
        display: flex;
        justify-content: space-between;
        margin-top: 20px;
    }
</style>